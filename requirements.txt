# Core
yfpy>=17.0.0
pandas>=2.2
numpy>=1.26
requests>=2.31
PyYAML>=6.0
python-dateutil>=2.9

# NFL historical data (nflverse successor to the deprecated nfl_data_py)
nflreadpy>=0.1.5

# Postgres backend (hosted deployments; SQLite needs no driver)
psycopg[binary]>=3.2

# Dashboard
streamlit>=1.40

# Dev
pytest>=8.0

# Static analysis (fcc check). Not needed to run the app.
ruff>=0.16
mypy>=1.11
vulture>=2.14
bandit>=1.8
